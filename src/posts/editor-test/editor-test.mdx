{/* WARNING: This file has been AI generated */}
import { ShaderPreview } from '../../components'
import Kofi from '../../assets/link-icons/kofi.png'

{/* Full shader — also shown as the code block */}
<ShaderPreview fullShader={`shader_type canvas_item;

void fragment() {
  vec2 uv = UV;
  COLOR = vec4(uv, abs(sin(TIME)), 1.0);
}`} />

{/* Full shader on top, snippet shown in the code block */}
<ShaderPreview
  layout="top"
  fullShader={`shader_type canvas_item;
void fragment() {
  COLOR = texture(TEXTURE, UV + vec2(sin(TIME * 2.0) * 0.05, 0.0));
}`}
>
{`// wobble the UV horizontally over time
UV + vec2(sin(TIME * 2.0) * 0.05, 0.0)`}
</ShaderPreview>

{/* Custom sprite with a tint — full shader runs, snippet shown */}
<ShaderPreview
  sprite={Kofi}
  fullShader={`shader_type canvas_item;
void fragment() {
  COLOR = texture(TEXTURE, UV) * vec4(1.0, 0.7, 0.7, 1.0);
}`}
>
{`// tint the sprite red
COLOR = texture(TEXTURE, UV) * vec4(1.0, 0.7, 0.7, 1.0);`}
</ShaderPreview>